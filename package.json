{
  "name": "scanmenu",
  "private": true,
  "version": "0.1.0",
  "description": "Scan Menu multilingual restaurant ordering platform.",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "predev": "npm run build -w @scanmenu/shared",
    "dev": "npm run dev --workspaces --if-present",
    "dev:web": "npm run dev -w @scanmenu/web -- -p 3000",
    "dev:mobile": "npm run dev -w @scanmenu/mobile",
    "dev:gateway": "npm run dev -w @scanmenu/api-gateway",
    "dev:auth": "npm run dev -w @scanmenu/auth-service",
    "dev:restaurants": "npm run dev -w @scanmenu/restaurant-service",
    "dev:orders": "npm run dev -w @scanmenu/order-service",
    "dev:translations": "npm run dev -w @scanmenu/translation-service",
    "dev:docker": "docker compose up --build",
    "dev:down": "docker compose down",
    "health": "node scripts/health-check.mjs",
    "pretypecheck": "npm run build -w @scanmenu/shared",
    "pretest": "npm run build -w @scanmenu/shared",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "@expo/metro-runtime": "~5.0.5",
    "react-native-web": "^0.20.0"
  }
}
